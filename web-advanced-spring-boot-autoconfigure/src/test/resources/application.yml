spring:
  application:
    name: web-test-application
web-advanced:
  security:
    enabled: false
    provider: AUTH0
    auth0:
      issuer: 'z'
      connection-name: 'p'
      sso-enable: true
      authenticationAPI:
        api-audience: 'x'
        client-id: 'y'
        client-secret: 'z'
      managementAPI:
        api-audience: 'x'
        client-id: 'y'
        client-secret: 'z'